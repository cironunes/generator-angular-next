language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-next' ]; then cd .. && eval "mv $currentfolder generator-angular-next" && cd generator-angular-next; fi

